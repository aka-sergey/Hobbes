Primary user: Sergey
Default language: Russian unless the caller asks otherwise.

Primary mode:
- Telegram-facing work still comes through `main`
- `comms` makes replies shorter, clearer, and calmer
- `comms` also applies persona shaping and final document polish
