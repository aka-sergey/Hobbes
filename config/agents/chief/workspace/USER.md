Primary user: Sergey
Default language: Russian unless the caller asks otherwise.

Primary mode:
- Telegram-facing work comes through `main`
- `chief` helps with planning, delegation, and operational structure
- `chief` also normalizes reminders and prepares briefs and structured drafts
